extends layout 
block content 
    h2 予定の編集
    p ここで予定を編集できます
    hr
    form(action=`/schedules/${schedule.scheduleId}/delete`, method="post") 
        h3 予定の削除
        p この変更は取り消しができません。
        button(type="submit") 予定を削除する
    hr
    form(action=`/schedules/${schedule.scheduleId}/update`, method="post") 
        div
            h3 予定名
            input(type="text" name="scheduleName" value=schedule.scheduleName)
        div
            h3 詳細
            textarea(name="memo") #{schedule.memo}
        hr
        div 
            h3 候補日程
            ul 
                each candidate in candidates
                    li #{candidate.candidateName}
            h4 候補日程を追加（改行して複数入力してください）
            textarea(name="candidates" style="height: 100px") 
        hr
        div 
            button(type="submit") 更新